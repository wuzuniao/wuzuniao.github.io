# AGENTS.md – 项目开发指南（AI 辅助编程专用）

> 为 AI 编程工具提供项目上下文与编码约束。所有自动生成的代码必须严格遵守本文档约定。

---

## 1. 项目概述

- 纯前端展示站点，技术栈 HTML5+CSS3+JavaScript。

---

## 2. 技术栈

- 仅使用 HTML5+CSS3+JavaScript；若引入框架/库，必须本地引入（禁止 CDN），页面可在浏览器中直接运行。

> **强制指令**：实现功能前优先查阅官方文档，严禁依赖训练数据中的过时知识。

---

## 3. 核心原则（减少 LLM 常见错误）

> **取向说明**：以下原则倾向于**稳健优先于速度**。对于琐碎任务（如修正拼写、单行改动），可适度灵活判断，但对于任何涉及业务逻辑或结构变更的任务，必须严格遵守。

### 3.1 先思考，后编码（Think Before Coding）
**不假设、不掩饰困惑、主动暴露权衡。**

- **明确假设**：在实现前，明确陈述你的假设。如果需求模糊，列出所有可能解释，等待澄清，不要擅自选择。
- **暴露权衡**：如果存在多种方案，简要说明优缺点，推荐最简方案。
- **拒绝过度设计**：不实现未明确要求的功能、抽象或配置项。
- **遇到困惑立即停止**：明确指出困惑点，并主动提问。

### 3.2 极简主义（Simplicity First）
**编写解决当前问题的最小代码量，不写投机性代码。**

- 不添加“未来可能需要”的扩展点、配置项或抽象层。
- 不编写单次使用场景下的通用框架。
- 不为不可能发生的错误编写处理逻辑。
- 如果代码超过 200 行，检查是否有简化空间（如拆分函数、消除重复）。
- **核心自检**：**“资深工程师会觉得这过于复杂吗？”** 如果是，请简化。

### 3.3 手术式修改（Surgical Changes）
**只改动必须改的地方。只清理自己引入的“垃圾”。**

- 修改现有代码时，**仅触及与任务直接相关的行**。
- 不“顺手”优化、重构或格式化无关代码，即使其风格与你习惯不符。
- 若发现预存在的技术债务（如未使用的导入、过时注释），**仅口头提醒，不主动清理**（除非明确要求）。
- **清理自己的痕迹**：删除自己的改动引入的未使用变量/导入，但不要删除预存在的死代码。
- **检验标准**：**每一行改动都应直接追溯至用户本次的具体请求。**

### 3.4 目标驱动执行

将任务转化为可验证目标，构建"验证循环"独立迭代：

| 任务 | 验证目标 |
|------|----------|
| 添加导航项 | 点击可正确跳转，二级菜单正常展开/收起 |
| 新增子页面 | 页面可访问，样式一致，导航页脚正常 |

---

## 4. 新会话初始化

1. 检查 `AGENTS.md` 是否存在且为最新版本。
2. 理解项目约束与编码规范，阅读相关现有代码遵循已有模式。

---

## 5. 编码规范

> 统一 **2 空格缩进**。**HTML/CSS/JS 必须严格文件分离**：禁止内联 `style`、内联 `<script>`、嵌入式 `<style>`，CSS 写入 `.css`，JS 写入 `.js`。

### 5.1 HTML

- `<!doctype html>`，`<html lang="zh-CN">`，`<meta charset="utf-8">` 置于 `<head>` 首部。
- viewport：`content="width=device-width,initial-scale=1.0"`。
- 语义化标签（`header`/`nav`/`main`/`footer`/`section`/`article`）。
- `<img>` 必须含 `alt`；外链必须 `target="_blank"`。

### 5.2 CSS

- 文件首行 `@charset "utf-8";`；类名 kebab-case，语义化（如 `pc-nav`、`m-footer`）。
- 优先 Flexbox/Grid；尺寸优先相对单位（`rem`/`em`/`%`/`vw`/`vh`）。
- 颜色用十六进制或 `rgba()`；避免 `!important`；媒体查询放文件末尾。

### 5.3 JavaScript

- ES6+ 语法（`const`/`let`、箭头函数、模板字符串、解构等）；禁止 `var`。
- 驼峰命名；原生 DOM API（`querySelector`/`addEventListener`），新代码不依赖 jQuery。
- 简体中文注释说明关键逻辑。

### 5.4 资源管理

- 图片置 `images/`（小写+下划线）；CSS 置 `style/`；JS 置 `script/`。
- 子目录页面资源路径用 `../`。

---

## 6. 成功标准与质量门禁

- 代码无多余实现，命名清晰无拼写错误，简体中文注释完整。
- 未引入新的语法或运行时错误，资源路径正确。
- HTML/CSS/JS 严格分离，无内联样式或脚本。
- **每次改动必须保证响应式**，PC 端与移动端布局均正常。

**任何项未通过，必须先修正再提交。**

---

## 7. AI 特别约束

- **禁止**：生成测试代码（除非明确要求）、引入图片/字体等二进制资源、使用已废弃的语法或特性、随意修改 `.gitignore` 或基础配置、引入无法离线获取的第三方依赖、在 HTML 中内联 CSS 或 JS。
- **必须**：使用简体中文交流（含注释、日志、提交信息）、每次改动保证响应式（PC 端与移动端均正常）、遵守 `DESIGN-vercel.md` 中的设计约定（若存在）。

---

## 8. 参考文档（优先级：高→低）

1. 官方文档——必须首先查阅
2. 本项目已有代码（遵循现有模式）
3. 社区最佳实践（仅当官方文档未覆盖时）

---

## 9. 提交与版本控制

- 格式：`<type>(<scope>): <subject>`，type：`feat`/`fix`/`docs`/`style`/`refactor`/`perf`/`chore`，subject 用简体中文。

---

**原则生效标志：** diff 中不必要的变更减少，过度复杂导致的重写减少，澄清问题在实现前提出。
